# Path to the SQLite database file
DATABASE_PATH=./data/library.db

# Path to the directory where uploaded book files are stored (local mode only)
LIBRARY_PATH=./data/library

# Secret key used by NextAuth.js for signing sessions
NEXTAUTH_SECRET=your-secret-here

# Public URL of the app.
# Web dev typically runs on :3000; Electron sets :3210 at runtime.
NEXTAUTH_URL=http://localhost:3000

# ---------- S3 / R2 Storage (optional) ----------
# Set S3_BUCKET to enable S3 mode. When set, the watcher polls the bucket
# instead of watching a local directory. Local mode and S3 mode are mutually
# exclusive — use one or the other.
#
# S3_ENDPOINT=https://<account-id>.r2.cloudflarestorage.com
# S3_REGION=auto
# S3_BUCKET=my-ebooks
# S3_ACCESS_KEY_ID=
# S3_SECRET_ACCESS_KEY=
# S3_PREFIX=
# S3_POLL_INTERVAL=60
